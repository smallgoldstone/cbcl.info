<%= simple_form_for @club, :validate => true do |f| %>
  	<%= f.input :name%>
  	<%= f.input :url%>
  	<%= f.input :desc%>

  <%= f.actions do %>
    <%= f.action :submit, :as => :input %>
  <% end %>
<% end %>
